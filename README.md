# KlajnDartsLearning
Průchod 15stupňovým C# roadmapem pro vývoj backendu šipkařské aplikace.


# Context

Tvoje zadání (Prompt pro nové chaty)Co zkopírovat na začátek nového chatu:"Ahoj, učím se C# backend pro svoji šipkařskou aplikaci (šipky 501, turnaje, offline-first logic, REST API v ASP.NET Core, EF Core, SQLite/PostgreSQL, JWT auth).Pracujeme podle 15stupňového roadmapu. Dnes chci detailně probrat ÚKOL ČÍSLO [DOPLŇ ČÍSLO].Chci si projít různé způsoby řešení, nápaditost architektury, čistotu kódu (Clean Code) a osvědčené postupy (Best Practices) v C#."Kompletní přehled 15 úkolů (Kopie pro tvé poznámky)Sekce A: C# Jazyk, OOP a Šipkařská Logika (Console App)#ÚkolHlavní C# koncepty, které si vyzkoušíš1Kalkulačka 1 hodustring parsing, switch expression / if-else, základní matematika.2Odečítací logika 501while loop, stavové proměnné, podmínky pro Bust (přestřel).3Detektor zavírání (Checkout)Dictionary, algoritmy na nalézání cest / kombinací do 170.4Správa hráčů v pamětiTřídy, objekty, List<T>, LINQ query (Where, OrderBy).5Generátor KO pavoukaPráce s poli, náhodné řazení (Random), párování prvků.Sekce B: Pokročilý C# a Datové operace#ÚkolHlavní C# koncepty, které si vyzkoušíš6JSON Ukládání stavuSystem.Text.Json (Serialization/Deserialization), File I/O.7Validace a Exception HandlingCustom Exception třídy, try-catch bloky, fail-fast princip.8Asynchronní simulátorasync / await, Task, Task.Delay, neblokující kód.Sekce C: ASP.NET Core Web API (HTTP End-pointy)#ÚkolHlavní C# koncepty, které si vyzkoušíš9Moje první REST APIASP.NET Core Minimal API / Controllers, HTTP GET, JSON response.10Výpočet statistik (POST)HTTP POST, Request Body parsing, DTO (Data Transfer Objects).11In-Memory Turnaje (CRUD)Dependency Injection (Singleton/Scoped), REST architektura.Sekce D: Databáze, Entity Framework a Zabezpečení#ÚkolHlavní C# koncepty, které si vyzkoušíš12EF Core & DatabázeDbContext, Migrace, SQLite/PostgreSQL, SaveChangesAsync().13Vztahy 1:N a M:N v EF CoreNavigační property, Foreign Keys, Include() na načítání relací.14Uživatelská AutentizaceHeslový Hashing (BCrypt/Argon2), Generování JWT Tokenů.15Zabezpečení End-pointůAtribut [Authorize], Middleware, overování tokenů v HTTP Headeru.